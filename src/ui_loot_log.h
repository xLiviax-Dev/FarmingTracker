#pragma once

namespace UILootLog
{
    // Renders the full "Loot Log" tab (sub-tabs: Live Log + Settings).
    // Called from ui.cpp inside the main tab bar.
    void RenderLootLogTab();

} // namespace UILootLog
